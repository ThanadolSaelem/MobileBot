sed -i 's/Text("Grant Overlay Permission", fontSize = 12.sp)/Text("Grant Overlay Permission", fontSize = 12.sp)\n                }\n                Spacer(modifier = Modifier.height(8.dp))\n                Button(\n                    onClick = { \n                        val intent = android.content.Intent(context, com.cfks.goosedroid.service.PetOverlayService::class.java)\n                        context.startService(intent)\n                    },\n                    modifier = Modifier.fillMaxWidth(),\n                    colors = ButtonDefaults.buttonColors(containerColor = Color(0xFF10B981))\n                ) {\n                    Text("Start Overlay Bot")\n                }\n                Button(\n                    onClick = { \n                        val intent = android.content.Intent(context, com.cfks.goosedroid.service.PetOverlayService::class.java)\n                        context.stopService(intent)\n                    },\n                    modifier = Modifier.fillMaxWidth(),\n                    colors = ButtonDefaults.buttonColors(containerColor = Color(0xFFEF4444))\n                ) {\n                    Text("Stop Overlay Bot")/g' app/src/main/java/com/cfks/goosedroid/ui/screens/SettingsScreen.kt
